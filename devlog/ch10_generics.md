# 第10章: ジェネリクス・トレイト・ライフタイム

## ジェネリクス（Generics）

**型の重複を排除！C++ のテンプレートと同じ！**

### 関数

```rust
fn largest<T>(list: &[T]) -> T { ... }
//        ↑ 型パラメータ
```

### 構造体

```rust
struct Point<T> {
    x: T,
    y: T,
}
```

### C++ との比較

| | C++ | Rust |
|---|-----|------|
| 構文 | `template<typename T>` | `<T>` |
| 例 | `vector<int>` | `Vec<i32>` |

```cpp
// C++
template<typename T>
T largest(std::vector<T> list) { ... }
```

```rust
// Rust
fn largest<T>(list: &[T]) -> T { ... }
```

### なぜ `impl<T>`？

```rust
struct Point<T> { x: T, y: T }

impl<T> Point<T> {
//   ↑ 「T はジェネリック型」と宣言
    fn x(&self) -> &T { &self.x }
}
```

**コンパイラに「T は具体的な型名じゃなくてジェネリック」と教える！**

---

## トレイト（Traits）

**「この機能を持ってます」という約束！**

### 定義

```rust
trait Animal {
    fn eat(&self);
    fn display(&self);
}
```

### 実装

```rust
impl Animal for Dog {
    fn eat(&self) { println!("犬が食べる"); }
    fn display(&self) { println!("犬だよ"); }
}
```

### なぜトレイト？

**違う型を同じように扱える！**

```rust
fn feed_all(animals: Vec<&dyn Animal>) {
    for a in animals {
        a.eat();  // Dog でも Cat でも OK！
    }
}
```

---

## 各言語との比較

### Java

```java
// インターフェース
interface Animal {
    void eat();
}

// 実装
class Dog implements Animal {
    public void eat() { ... }
}

// 使う
void feedAll(List<Animal> animals) { ... }
```

### Rust

```rust
// トレイト
trait Animal {
    fn eat(&self);
}

// 実装
impl Animal for Dog {
    fn eat(&self) { ... }
}

// 使う
fn feed_all(animals: Vec<&dyn Animal>) { ... }
```

### C++

```cpp
// 抽象クラス
class Animal {
public:
    virtual void eat() = 0;  // 純粋仮想関数
};

// 実装
class Dog : public Animal {
    void eat() override { ... }
};
```

---

## Rust とオブジェクト指向

| OOPの特徴 | Java | C++ | Rust |
|-----------|------|-----|------|
| カプセル化 | ✅ | ✅ | ✅ |
| 多態性 | ✅ | ✅ | ✅（トレイト）|
| 継承 | ✅ | ✅ | **❌ なし！** |

**Rust は「継承のないオブジェクト指向」！**

### なぜ継承がない？

**1. ダイヤモンド問題**

```
       Animal
       /    \
     Dog    Cat
       \    /
       DogCat  ← 💀 Animal が2つある！
```

**2. 継承は密結合**

親を変えると子が全部壊れる😭

**3. 継承より合成（Composition over Inheritance）**

```rust
// 継承じゃなくて「持つ」
struct Dog {
    animal_data: AnimalData,
}
```

**4. トレイトで解決**

```rust
trait Walkable { fn walk(&self); }
trait Swimmable { fn swim(&self); }

// 複数トレイトを実装できる！ダイヤモンド問題なし！
impl Walkable for Duck { ... }
impl Swimmable for Duck { ... }
```

| 問題 | 継承 | トレイト |
|------|------|---------|
| ダイヤモンド問題 | 💀 | ✅ なし |
| 密結合 | 😭 | ✅ 疎結合 |
| 柔軟性 | 低い | 高い |

**継承は便利に見えて罠が多い！トレイトの方が安全！**

---

## トレイト境界

**「この型はこのトレイトを実装してる」という制約**

```rust
fn largest<T: PartialOrd>(list: &[T]) -> T {
//           ↑ 「比較できる型だけOK」
    ...
}
```

### 複数のトレイト境界

```rust
fn some_function<T: Display + Clone>(t: T) { ... }
```

### where 構文

```rust
fn some_function<T, U>(t: T, u: U)
where
    T: Display + Clone,
    U: Clone,
{ ... }
```

---

## 💡 学んだこと

- **ジェネリクス** = 型パラメータで重複排除
- **トレイト** = 「機能の約束」（Java の interface 相当）
- **Rust に継承はない！** トレイトで機能を組み合わせる
- **トレイト境界** = 「この機能を持つ型だけ」という制約
- `impl<T>` = 「T はジェネリック型です」宣言

---

## TODO

- ライフタイムの詳細
